pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        jcenter()
        gradlePluginPortal()
    }
}

rootProject.name = 'assertk'

include 'java-interop'

enableFeaturePreview('GRADLE_METADATA')
